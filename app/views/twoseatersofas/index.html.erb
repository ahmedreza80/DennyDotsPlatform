<p id="notice"><%= notice %></p>

<h1>Twoseatersofas</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th>Price</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @twoseatersofas.each do |twoseatersofa| %>
      <tr>
        <td><%= twoseatersofa.title %></td>
        <td><%= twoseatersofa.description %></td>
        <td><%= twoseatersofa.price %></td>
        <td><%= link_to 'Show', twoseatersofa %></td>
        <td><%= link_to 'Edit', edit_twoseatersofa_path(twoseatersofa) %></td>
        <td><%= link_to 'Destroy', twoseatersofa, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Twoseatersofa', new_twoseatersofa_path %>

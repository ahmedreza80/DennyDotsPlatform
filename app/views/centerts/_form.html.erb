<%= form_with(model: centert, local: true) do |form| %>
  <% if centert.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(centert.errors.count, "error") %> prohibited this centert from being saved:</h2>

      <ul>
      <% centert.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :centert_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :centert_description %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price, id: :centert_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%= form_with(model: sofacumb, local: true) do |form| %>
  <% if sofacumb.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sofacumb.errors.count, "error") %> prohibited this sofacumb from being saved:</h2>

      <ul>
      <% sofacumb.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :sofacumb_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :sofacumb_description %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price, id: :sofacumb_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

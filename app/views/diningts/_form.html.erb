<%= form_with(model: diningt, local: true) do |form| %>
  <% if diningt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(diningt.errors.count, "error") %> prohibited this diningt from being saved:</h2>

      <ul>
      <% diningt.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :diningt_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :diningt_description %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price, id: :diningt_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

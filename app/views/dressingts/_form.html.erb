<%= form_with(model: dressingt, local: true) do |form| %>
  <% if dressingt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dressingt.errors.count, "error") %> prohibited this dressingt from being saved:</h2>

      <ul>
      <% dressingt.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :dressingt_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :dressingt_description %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price, id: :dressingt_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

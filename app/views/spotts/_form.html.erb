<%= form_with(model: spott, local: true) do |form| %>
  <% if spott.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(spott.errors.count, "error") %> prohibited this spott from being saved:</h2>

      <ul>
      <% spott.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :spott_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :spott_description %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price, id: :spott_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

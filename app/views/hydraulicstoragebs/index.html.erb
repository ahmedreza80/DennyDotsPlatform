<p id="notice"><%= notice %></p>

<h1>Hydraulicstoragebs</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th>Price</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @hydraulicstoragebs.each do |hydraulicstorageb| %>
      <tr>
        <td><%= hydraulicstorageb.title %></td>
        <td><%= hydraulicstorageb.description %></td>
        <td><%= hydraulicstorageb.price %></td>
        <td><%= link_to 'Show', hydraulicstorageb %></td>
        <td><%= link_to 'Edit', edit_hydraulicstorageb_path(hydraulicstorageb) %></td>
        <td><%= link_to 'Destroy', hydraulicstorageb, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Hydraulicstorageb', new_hydraulicstorageb_path %>

<%= form_with(model: doubleb, local: true) do |form| %>
  <% if doubleb.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(doubleb.errors.count, "error") %> prohibited this doubleb from being saved:</h2>

      <ul>
      <% doubleb.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :doubleb_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :doubleb_description %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price, id: :doubleb_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%= form_with(model: tvcabinet, local: true) do |form| %>
  <% if tvcabinet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tvcabinet.errors.count, "error") %> prohibited this tvcabinet from being saved:</h2>

      <ul>
      <% tvcabinet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :tvcabinet_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :tvcabinet_description %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price, id: :tvcabinet_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

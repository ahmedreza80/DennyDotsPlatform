<%= form_with(model: kidstudyt, local: true) do |form| %>
  <% if kidstudyt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kidstudyt.errors.count, "error") %> prohibited this kidstudyt from being saved:</h2>

      <ul>
      <% kidstudyt.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :kidstudyt_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :kidstudyt_description %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price, id: :kidstudyt_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%= form_with(model: singleb, local: true) do |form| %>
  <% if singleb.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(singleb.errors.count, "error") %> prohibited this singleb from being saved:</h2>

      <ul>
      <% singleb.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :singleb_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :singleb_description %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price, id: :singleb_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
